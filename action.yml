name: 'Action Agogo!'
description: 'Trying out container-based actions -- do not use!'
author: 'Joshua M. Clulow <josh@sysmgr.org>'
inputs: 
  colour:
    description: 'What colour should it be?'
    default: 'green'
runs:
  using: 'docker'
  image: 'docker://jclulow/buildomat:latest'
  args:
    - ${{ inputs.colour }}
  env:
    WHOM: 'THEE!'
    COLOUR_ENV: ${{ inputs.colour }}
